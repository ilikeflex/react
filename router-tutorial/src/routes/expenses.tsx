import React from "react"

export const Expenses: React.FC<unknown> = (props:unknown) => {
    return (
        <main style={{ padding: "1rem 0" }}>
          <h2>Expenses</h2>
        </main>
    )
}