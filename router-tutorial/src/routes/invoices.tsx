import React from "react"

export const Invoices:React.FC<unknown> =  (props:unknown) => {
    return (
        <main style={{ padding: "1rem 0" }}>
            <h2>InvoiceSS Component:</h2>;
        </main>
    )
}